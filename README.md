# Todo-List-Test

to run 
```
1. change [DATABASE_NAME] and [DATABASE_PASSWORD] to fit in packages/graphql-server/prisma/schema.prisma
2. optional. if needed change ApolloClient uri in packages/react-app/src/utils/ApolloClient.ts
3. yarn run setup
4. yarn run start
```
